import feed from "../feed";

export const {
    createEntity,
    fetchFeed,
    fetchEntity,
    setFilters,
} = feed.cells;

export default {
    createEntity,
    fetchFeed,
    fetchEntity,
    setFilters,
};