type Query {
  validateInvitation(email: String, linkId: String): Boolean
}

type Mutation {
  acceptInvitation(email: String, linkId: String): Boolean
  inviteApplication(input: InvitationApplicationInput!): Boolean
}

input InvitationApplicationInput {
  email: String!
  name: String
  text: String
}

